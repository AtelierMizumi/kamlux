# Cloudflare Pages Redirects
# This file handles routing for static pages

# Redirect root to index.html is handled automatically

# All routes work without .html extension
# /about -> /about.html
# /install -> /install.html
